/*
 *@Description : B2031 计算三角形面积
 *@Author : Zesheng Wang
 *@Date : 2022/4/13 15:23
 *@Version : 1.0
*/
/*
#include <iostream>
#include <cstdio>
#include <cmath>

using namespace std;

int main() {
    double x1, x2, x3, y1, y2, y3;
    double len1, len2, len3, p, s;
    scanf("%lf %lf %lf %lf %lf %lf", &x1, &y1, &x2, &y2, &x3, &y3);
    len1 = sqrt(pow(y2 - y1, 2) + pow(x2 - x1, 2));
    len2 = sqrt(pow(y3 - y1, 2) + pow(x3 - x1, 2));
    len3 = sqrt(pow(y3 - y2, 2) + pow(x3 - x2, 2));
    p = (len1 + len2 + len3) / 2;
    s = sqrt(p * (p - len1) * (p - len2) * (p - len3));
    printf("%.2lf", s);
    return 0;
}
 */
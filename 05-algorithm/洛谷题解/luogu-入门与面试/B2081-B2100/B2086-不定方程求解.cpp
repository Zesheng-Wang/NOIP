/*
 *@Description : B2086 不定方程求解
 *@Author : Zesheng Wang
 *@Date : 2022/6/24 20:18
 *@Version : 1.0
*/
/*
#include <iostream>
#include <cstdio>

using namespace std;

#define MAX 10000

int main() {
    int a, b, c,count=0;
    scanf("%d %d %d", &a, &b, &c);
    for (int x = 0; x < MAX; x++) {
        for (int y = 0; y < MAX; y++) {
            if (a * x + b * y == c && x == int(x) && y == int(y)) {
                count++;
            }
        }
    }
    printf("%d", count);
    return 0;
}
 */
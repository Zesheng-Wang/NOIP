/*
 *@Description : B2066 救援
 *@Author : Zesheng Wang
 *@Date : 2022/6/14 13:40
 *@Version : 1.0
*/
/*
#include <iostream>
#include <cstdio>
#include <cmath>

using namespace std;

int main() {
    int n, p;
    double x, y, s, t = 0.0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%lf %lf %d", &x, &y, &p);
        s = sqrt(x * x + y * y);
        t += s * 2 * 1.0 / 50 + p * 1.5;
    }
    t = ceil(t);
    printf("%d", int(t));
    return 0;
}
 */